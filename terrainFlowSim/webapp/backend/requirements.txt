# 웹 백엔드 추가 의존성.
# torch / torchvision / matplotlib / numpy / pillow / scipy 는
# ai_model/.venv 에 이미 설치돼 있으므로 여기서는 웹 서버 + 인증 관련만 명시한다.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 회원가입/로그인 (MySQL)
sqlalchemy>=2.0
pymysql>=1.1
bcrypt>=4.0
